name = "free-proxy-sub"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV 命名空间（用于缓存代理列表）
[[kv_namespaces]]
binding = "PROXY_KV"
preview_id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# 定时触发器（可选，每小时更新一次）
# [[triggers.cron]]
# schedule = "0 * * * *"
