name = "free-proxy-sub"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV 命名空间（用于缓存代理列表）
# 注意：实际部署时需要在 Cloudflare Dashboard 创建 KV 并填写 id
[[kv_namespaces]]
binding = "PROXY_KV"
# preview_id = "preview_id_here"

# 定时触发器（可选，每小时更新一次）
# [[triggers.cron]]
# schedule = "0 * * * *"
